name: Push to Public Repo

on:
  push:
    branches:
      - test-one

jobs:
  push_to_public_repo:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo A
        uses: actions/checkout@v2
        with:
          repository: balaji-tynybay/HTML-games
          ref: test-one
        env:
          ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }}

      - name: Set up Git
        run: |
          git config --global user.name "Your Name"
          git config --global user.email "your@email.com"

      - name: Clone Repo B
        run: |
          git clone https://github.com/bbalaji13/html-games.git repo_b
          cd repo_b
          git checkout -b test-one
          cp -r ../${{ github.repository }}/* .

      - name: Commit and Push to Repo B
        run: |
          git add .
          git commit -m "Merge changes from test-one branch"
          git push origin test-one
        working-directory: ./repo_b
        env:
          ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }}
